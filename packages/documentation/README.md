# Documentation

Detailed docs for motionwind that don't belong in the top-level README.

- **[features.md](./features.md)** — full syntax and feature reference: gestures, properties,
  transitions, filters, scroll-linked animations, variants, dynamic classNames, packages, and
  tooling (migrate / ESLint / Prettier).
- **[architecture.md](./architecture.md)** — how it works internally: monorepo layout, the
  build-time → runtime data flow, the parser, framework integration, the static-vs-dynamic
  paths, and a contributing guide.

For end-user guides and the live playground, see [motionwind.xyz](https://motionwind.xyz).
