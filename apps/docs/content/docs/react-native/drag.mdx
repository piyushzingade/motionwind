---
title: "Drag"
description: "Draggable elements with axis locking, elasticity, and snap-back."
---

# Drag

motionwind-react-native supports drag gestures through `animate-drag-*` classes.

## Enable drag

<RNPreview title="Draggable Elements">
  <div style={{display: 'flex', gap: 10, alignItems: 'center'}}>
    <AnimBox animation="scaleIn" delay={0}>
      <div style={{width: 52, height: 52, background: '#6366f1', borderRadius: 14, display: 'flex', alignItems: 'center', justifyContent: 'center', cursor: 'grab'}}>
        <span style={{color: '#fff', fontSize: 8, fontWeight: 600}}>Both</span>
      </div>
    </AnimBox>
    <AnimBox animation="scaleIn" delay={100}>
      <div style={{width: 52, height: 52, background: '#0ea5e9', borderRadius: 14, display: 'flex', alignItems: 'center', justifyContent: 'center', cursor: 'grab'}}>
        <span style={{color: '#fff', fontSize: 8, fontWeight: 600}}>X only</span>
      </div>
    </AnimBox>
    <AnimBox animation="scaleIn" delay={200}>
      <div style={{width: 52, height: 52, background: '#10b981', borderRadius: 14, display: 'flex', alignItems: 'center', justifyContent: 'center', cursor: 'grab'}}>
        <span style={{color: '#fff', fontSize: 8, fontWeight: 600}}>Y only</span>
      </div>
    </AnimBox>
  </div>
</RNPreview>

## Snap back to origin

```tsx
<mw.View className="animate-drag-both animate-drag-snap bg-indigo-500 w-20 h-20 rounded-xl" />
```

After the user releases, the element springs back to its original position.

## Elasticity

Control resistance when dragged beyond constraints:

```tsx
// 50% elasticity
<mw.View className="animate-drag-both animate-drag-elastic-50" />

// No elasticity (hard stop)
<mw.View className="animate-drag-both animate-drag-elastic-0" />

// Full rubber band
<mw.View className="animate-drag-both animate-drag-elastic-100" />
```

## Drag constraints

Limit how far the element can be dragged:

<RNPreview title="Constrained Drag">
  <AnimBox animation="spring">
    <div style={{width: 56, height: 56, background: '#c8ff2e', borderRadius: 16, display: 'flex', alignItems: 'center', justifyContent: 'center', cursor: 'grab', boxShadow: '0 0 20px rgba(200,255,46,0.3)'}}>
      <span style={{color: '#0a0a0f', fontSize: 9, fontWeight: 700}}>Drag</span>
    </div>
  </AnimBox>
</RNPreview>

| Class | Direction |
|---|---|
| `animate-drag-constraint-t-{n}` | Max drag up |
| `animate-drag-constraint-b-{n}` | Max drag down |
| `animate-drag-constraint-l-{n}` | Max drag left |
| `animate-drag-constraint-r-{n}` | Max drag right |

## Disable momentum

```tsx
<mw.View className="animate-drag-both animate-drag-no-momentum" />
```

## Combining drag with entrance

<RNPreview title="Entrance + Drag">
  <AnimBox animation="spring">
    <div style={{background: '#8b5cf6', padding: '14px 20px', borderRadius: 14, cursor: 'grab'}}>
      <span style={{color: '#fff', fontSize: 12, fontWeight: 600}}>Springs in, then draggable</span>
    </div>
  </AnimBox>
</RNPreview>

## Next steps

- [Hooks](/docs/react-native/hooks) — Build custom drag interactions with `useMotionwind`
- [API Reference](/docs/react-native/api-reference) — Complete type definitions
